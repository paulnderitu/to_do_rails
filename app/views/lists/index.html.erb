<h1>Lists</h1>

  <ul>
    <% @lists.each do |list| %>
      <li><%= link_to list.name, list_path(list) %></li>
    <% end %>
  </ul>
<hr>
<button class=""><%= link_to "New List", new_list_path %></button>
